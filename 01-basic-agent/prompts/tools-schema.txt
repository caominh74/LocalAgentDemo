[
  {
    "type": "function",
    "function": {
      "name": "read",
      "description": "Read file contents from the workspace filesystem.",
      "parameters": {
        "type": "object",
        "properties": {
          "path": {
            "type": "string",
            "minLength": 1,
            "description": "Relative path to the file within the workspace (e.g. 'sample.txt', './package.json')."
          },
          "offset": {
            "type": "integer",
            "minimum": 1,
            "description": "Optional 1-indexed line offset to start reading from."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 5000,
            "description": "Optional maximum number of lines to read."
          }
        },
        "required": ["path"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "write",
      "description": "Write or overwrite content to a file, creating parent directories if needed.",
      "parameters": {
        "type": "object",
        "properties": {
          "path": {
            "type": "string",
            "minLength": 1,
            "description": "Relative path to the file to create or overwrite."
          },
          "content": {
            "type": "string",
            "description": "Text content to write into the file."
          }
        },
        "required": ["path", "content"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "edit",
      "description": "Perform surgical search-and-replace of an exact string chunk in a file.",
      "parameters": {
        "type": "object",
        "properties": {
          "path": {
            "type": "string",
            "minLength": 1,
            "description": "Relative path to the file to edit (e.g. './package.json')."
          },
          "oldText": {
            "type": "string",
            "minLength": 1,
            "description": "Exact non-empty text substring to be replaced. Must contain at least 1 character."
          },
          "newText": {
            "type": "string",
            "description": "New replacement text substring."
          }
        },
        "required": ["path", "oldText", "newText"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "bash",
      "description": "Execute a command in the host shell (PowerShell on Windows, bash on macOS/Linux). On Windows prefer PowerShell cmdlets such as Remove-Item and Get-Date.",
      "parameters": {
        "type": "object",
        "properties": {
          "command": {
            "type": "string",
            "minLength": 1,
            "description": "Command string for the host shell (PowerShell on Windows, bash on macOS/Linux)."
          },
          "timeout": {
            "type": "integer",
            "minimum": 100,
            "maximum": 120000,
            "description": "Optional maximum execution time in milliseconds (default 30000)."
          }
        },
        "required": ["command"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "list_dir",
      "description": "Inspect directory structure and file tree.",
      "parameters": {
        "type": "object",
        "properties": {
          "path": {
            "type": "string",
            "minLength": 1,
            "description": "Relative path to the directory to inspect (defaults to '.')."
          },
          "depth": {
            "type": "integer",
            "minimum": 1,
            "maximum": 10,
            "description": "Maximum recursion depth as an integer between 1 and 10. Do not pass strings like 'maximum'."
          }
        },
        "required": ["path"]
      }
    }
  }
]
