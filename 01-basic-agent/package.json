{
  "name": "01-basic-agent",
  "private": true,
  "scripts": {
    "dev": "bunx mprocs --names \"server,web\" \"bun run --cwd server dev\" \"bun run --cwd web dev\"",
    "server": "bun run --cwd server dev",
    "web": "bun run --cwd web dev",
    "demo:1:server": "bun run --cwd server dev",
    "demo:1:web": "bun run --cwd web dev"
  }
}
