# Demo 2 Sandbox Workspace

This directory serves as the isolated base directory (`cwd`) for all tools (`read`, `write`, `edit`, `bash`, `list_dir`) invoked by the agent in **Demo 2 (`02-hardened-agent`)**.

Drop any files, scripts, or datasets here for testing without affecting repository source files. Paths outside this directory will be rejected by the security guard.
